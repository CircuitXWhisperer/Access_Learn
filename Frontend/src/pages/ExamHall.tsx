import { useState } from 'react';
export function ExamHall(){ const [q,setQ]=useState(1); const [submitted,setSubmitted]=useState(false); const questions=['Which statement best describes inertia?','Which force changes an object’s velocity?','If net force is zero, what happens to velocity?']; return <div><div className="exam-top"><div><span className="eyebrow">EXAM HALL</span><h1>Monthly Science Assessment</h1><span>Class 10 • 45 minutes remaining</span></div><div className="exam-timer">00:{45-q*3}:12</div></div><div className="exam-layout"><div className="card question-card"><div className="question-head"><span>Question {q} of 3</span><div className="progress"><span style={{width:`${q/3*100}%`}}/></div></div><h2>{questions[q-1]}</h2><div className="options">{['An object resists changes in its state of motion','An object always moves in a circle','Force is always zero','Objects cannot change direction'].map((o,i)=><label key={o} className="option"><input type="radio" name="q" defaultChecked={i===0&&q===1}/><span>{String.fromCharCode(65+i)}</span>{o}</label>)}</div><div className="question-actions"><button className="btn secondary" disabled={q===1} onClick={()=>setQ(v=>v-1)}>← Previous</button>{q<3?<button className="btn primary" onClick={()=>setQ(v=>v+1)}>Next question →</button>:<button className="btn primary" onClick={()=>setSubmitted(true)}>Submit exam</button>}</div></div><aside className="card exam-tools"><h3>Accessibility Tools</h3><button>🔊 Read question aloud</button><button>✦ Simplify language</button><button>T+ Bigger text</button><button>◉ Focus mode</button><hr/><h3>Question map</h3><div className="qmap">{[1,2,3].map(n=><button key={n} className={n===q?'active':''} onClick={()=>setQ(n)}>{n}</button>)}</div></aside></div>{submitted&&<div className="modal-backdrop"><div className="card modal"><h2>Exam submitted</h2><p>Your responses were recorded successfully.</p><button className="btn primary" onClick={()=>setSubmitted(false)}>Return to exam hall</button></div></div>}</div> }
