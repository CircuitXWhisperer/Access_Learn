import { useStudents } from '../features/api';
export function Inclusion(){ const {data}=useStudents(); return <div><div className="page-heading"><div><span className="eyebrow">STUDENT INCLUSION ROSTER</span><h1>Support every learner</h1><p>Review accessibility profiles, progress and follow-up actions.</p></div><button className="btn secondary">Export accessible report</button></div><div className="card"><div className="table-toolbar"><input placeholder="Search students…"/><select defaultValue="all"><option value="all">All support types</option><option>Audio-first</option><option>Screen reader</option><option>Sign language</option></select><button className="btn secondary">Filters</button></div><div className="wide-table"><div className="table-head"><span>Student</span><span>Class</span><span>Support</span><span>Progress</span><span>Status</span><span>Action</span></div>{data?.map((s)=><div className="table-row" key={s.name}><span><b>{s.name}</b></span><span>{s.className}</span><span>{s.support}</span><span><div className="mini-progress"><span style={{width:`${s.progress}%`}}/></div>{s.progress}%</span><span className={s.status==='On track'?'status good':'status warn'}>{s.status}</span><button className="text-btn">Review</button></div>)}</div></div></div> }
