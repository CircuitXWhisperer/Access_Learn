import { useSchemes } from '../features/api';

export function Schemes(){ const {data,isLoading}=useSchemes(); return <div><div className="page-heading"><div><span className="eyebrow">NATIONAL SCHEMES HUB</span><h1>Benefits made easier</h1><p>Explore education, scholarship and accessibility schemes with simple next steps.</p></div><button className="btn primary">My Scheme Tracker</button></div><div className="scheme-categories"><div className="category active">Scholarships <span>12</span></div><div className="category">Accessibility <span>8</span></div><div className="category">Learning <span>17</span></div><div className="category">Exams <span>5</span></div></div><div className="scheme-grid">{isLoading?<div className="skeleton lg"/>:data?.map((s)=><article className="card scheme-card" key={s.name}><div className="scheme-icon">₹</div><span className="tag">{s.category}</span><h2>{s.name}</h2><p>{s.description}</p><div className="scheme-meta"><span>Deadline</span><strong>{s.deadline}</strong></div><button className="btn secondary full">View eligibility →</button></article>)}</div><div className="card resource-strip"><div><span className="eyebrow">LEARNING RESOURCES</span><h2>Inclusive NIOS Courses & SWAYAM / NPTEL</h2><p>Simplified guides, accessible formats and application walkthroughs.</p></div><button className="btn primary">Explore resources</button></div></div> }
